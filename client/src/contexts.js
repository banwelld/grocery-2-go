// contexts.js

import { createContext } from "react";

export const UserContext = createContext(null);
export const OpenOrderContext = createContext(null);
export const ItemContext = createContext(null);
